# Machine-specific configuration
# Copy this file to local.nix and customize for your machine
# local.nix is git-ignored and should not be committed
{
  # Your username on this machine
  username = "ville";

  # Your home directory
  homeDirectory = "/home/ville";

  # System architecture
  # Options: "x86_64-linux", "aarch64-linux", "x86_64-darwin", "aarch64-darwin"
  system = "x86_64-linux";

  # Git configuration
  gitUser = "Ville Rasanen";
  gitEmail = "git@vjm.anonaddy.me";
  gitSigningKey = "~/.ssh/id_rsa.pub";

  # Set to true if running in WSL
  isWsl = true;

  # Optional: override if you cloned the repo somewhere other than ~/dotfiles
  # dotfilesPath = "/home/ville/dotfiles";
}
