#!/usr/bin/env bash
set -euo pipefail

length="${1:-32}"

if ! [[ $length =~ ^[0-9]+$ ]]; then
	echo "Usage: genpass [length]" >&2
	echo "Default length: 32" >&2
	exit 1
fi

openssl rand -base64 $((length * 3 / 4 + 1)) | tr -d '\n=' | head -c "$length"
echo
